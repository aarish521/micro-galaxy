# Micro Galaxy

Micro Galaxy is an experimental AI agent system.

It takes a goal, breaks it into tasks, and executes them.

## Setup

Install dependencies:

pip install -r requirements.txt

Install Ollama and run a model:

ollama run phi

Run the system:

python main.py
